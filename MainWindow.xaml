<Window x:Class="AiWPFDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AiWPFDemo"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>

        <!-- Root folder selection -->
        <DockPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="8">
            <TextBlock Text="Root Folder:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Text="{Binding RootFolderPath}" IsReadOnly="True" Width="480" Margin="0,0,8,0"/>
            <Button Content="Browse..." Command="{Binding BrowseRootFolderCommand}" Width="120"/>
        </DockPanel>

        <!-- List area (3) -->
        <Grid Grid.Row="1" Grid.Column="0" Margin="8,0,8,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <GroupBox Header="Folder List" Grid.Row="0" Margin="0,0,0,8">
                <ListBox ItemsSource="{Binding FolderItems}"
                         SelectedItem="{Binding SelectedFolder}"
                         DisplayMemberPath="Name"/>
            </GroupBox>

            <GroupBox Header="WorkItem List" Grid.Row="1" Margin="0,0,0,8">
                <ListBox ItemsSource="{Binding WorkItems}"
                         SelectedItem="{Binding SelectedWorkItem}"
                         DisplayMemberPath="Name"/>
            </GroupBox>

            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <TextBlock Text="PDF Type:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <RadioButton Content="Image" IsChecked="{Binding IsImageSelected}"/>
                <RadioButton Content="Drawing" IsChecked="{Binding IsDrawingSelected}" Margin="8,0,0,0"/>
                <RadioButton Content="Model" IsChecked="{Binding IsModelSelected}" Margin="8,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- WebView2 area (7) -->
        <Border Grid.Row="1" Grid.Column="1" Margin="0,0,8,8"
                BorderBrush="#C7CBD1" BorderThickness="1" CornerRadius="6"
                Background="#F7F8FA">
            <Grid Margin="8">
                <TextBlock Text="PDF Display Area"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="#6B7280"/>
                <wv2:WebView2 Source="{Binding PdfSource}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
